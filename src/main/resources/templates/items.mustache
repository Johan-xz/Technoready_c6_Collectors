<h2>Artículos Coleccionables Disponibles</h2>

<div class="item-list">
    {{#items}}
        <div class="item-card">
            <h3><a href="/items-web/{{id}}">{{name}}</a></h3>
            <p>Categoría: {{category}}</p>
            <p>Precio: ${{price}}</p>
            {{#available}}
                <span class="status-available">Disponible</span>
            {{/available}}
            {{^available}}
                <span class="status-unavailable">Agotado</span>
            {{/available}}
        </div>
    {{/items}}
    {{^items}}
        <p>No hay artículos disponibles en este momento.</p>
    {{/items}}
</div>